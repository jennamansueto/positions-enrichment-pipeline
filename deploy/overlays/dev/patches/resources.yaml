apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-pipeline
spec:
  template:
    spec:
      containers:
        - name: pipeline
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1"
