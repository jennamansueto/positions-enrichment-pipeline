apiVersion: apps/v1
kind: Deployment
metadata:
  name: enterprise-pipeline
spec:
  template:
    spec:
      containers:
        - name: pipeline
          resources:
            requests:
              memory: "8Gi"
              cpu: "4"
            limits:
              memory: "16Gi"
              cpu: "8"
